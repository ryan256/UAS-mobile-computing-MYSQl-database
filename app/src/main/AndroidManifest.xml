<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ferdi.koperasi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".recycleview"></activity>
        <activity android:name=".Circle_menu" />
        <activity android:name=".cardview" />
        <activity android:name=".TampilanSemuaPgw" />
        <activity android:name=".TampilanPegawai" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Sign" />
        <activity android:name=".TampilanLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>